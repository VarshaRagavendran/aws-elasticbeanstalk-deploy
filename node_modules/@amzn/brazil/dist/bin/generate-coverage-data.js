#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const commander_1 = require("commander");
const __1 = require("../");
const __2 = require("../");
const program = new commander_1.Command();
const filenames = __1.coverageReporters.map((c) => c.filename);
program
    .description("copy coverage data to brazil locations.\n" +
    ` - copies ${filenames.join(", ")} to brazil-documentation for coverlay\n` +
    ` - writes coverage-data.txt for code.a.c badges`)
    .argument("[coverageDir]", "path to coverage files")
    .option("-l, --language <language>", "SLOCCount supported language")
    .action((coverageDir, args) => (0, __2.generateCoverageData)({ coverageDir, ...args }))
    .parseAsync(process.argv);
