"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = require("fs");
const path_1 = require("path");
const workspace = __importStar(require("../workspace"));
describe(".srcDir", () => {
    test("is a string", () => {
        expect(typeof workspace.srcDir).toBe("string");
    });
    test("is an absolute path", () => {
        expect((0, path_1.isAbsolute)(workspace.srcDir)).toBe(true);
    });
    test("ends with expected value", () => {
        expect(workspace.srcDir).toMatch(/src\/NodeJS-Brazil/);
    });
    test("exists as a directory", () => {
        expect((0, fs_1.statSync)(workspace.srcDir).isDirectory()).toBe(true);
    });
});
describe(".buildDir", () => {
    test("is a string", () => {
        expect(typeof workspace.buildDir).toBe("string");
    });
    test("is an absolute path", () => {
        expect((0, path_1.isAbsolute)(workspace.buildDir)).toBe(true);
    });
    test("ends with expected value", () => {
        expect(workspace.buildDir).toMatch(/src\/NodeJS-Brazil\/build/);
    });
    test("exists as a directory", () => {
        expect((0, fs_1.statSync)(workspace.buildDir).isDirectory()).toBe(true);
    });
});
describe(".libDir", () => {
    test("is a string", () => {
        expect(typeof workspace.libDir).toBe("string");
    });
    test("is an absolute path", () => {
        expect((0, path_1.isAbsolute)(workspace.libDir)).toBe(true);
    });
    test("ends with expected value", () => {
        expect(workspace.libDir).toMatch(/src\/NodeJS-Brazil\/build\/lib/);
    });
});
describe(".privateDir", () => {
    test("is a string", () => {
        expect(typeof workspace.privateDir).toBe("string");
    });
    test("is an absolute path", () => {
        expect((0, path_1.isAbsolute)(workspace.privateDir)).toBe(true);
    });
    test("ends with expected value", () => {
        expect(workspace.privateDir).toMatch(/src\/NodeJS-Brazil\/build\/private/);
    });
});
describe(".documentationDir", () => {
    test("is a string", () => {
        expect(typeof workspace.documentationDir).toBe("string");
    });
    test("is an absolute path", () => {
        expect((0, path_1.isAbsolute)(workspace.documentationDir)).toBe(true);
    });
    test("ends with expected value", () => {
        expect(workspace.documentationDir).toMatch(/src\/NodeJS-Brazil\/build\/brazil-documentation/);
    });
});
describe(".testOutputDir", () => {
    test("is a string", () => {
        expect(typeof workspace.testOutputDir).toBe("string");
    });
    test("is an absolute path", () => {
        expect((0, path_1.isAbsolute)(workspace.testOutputDir)).toBe(true);
    });
    test("ends with expected value", () => {
        expect(workspace.testOutputDir).toMatch(/src\/NodeJS-Brazil\/build\/brazil-unit-tests/);
    });
});
describe(".integOutputDir", () => {
    test("is a string", () => {
        expect(typeof workspace.integOutputDir).toBe("string");
    });
    test("is an absolute path", () => {
        expect((0, path_1.isAbsolute)(workspace.integOutputDir)).toBe(true);
    });
    test("ends with expected value", () => {
        expect(workspace.integOutputDir).toMatch(/src\/NodeJS-Brazil\/build\/brazil-integ-tests/);
    });
});
describe(".generatedMakeDir", () => {
    test("is a string", () => {
        expect(typeof workspace.generatedMakeDir).toBe("string");
    });
    test("is an absolute path", () => {
        expect((0, path_1.isAbsolute)(workspace.generatedMakeDir)).toBe(true);
    });
    test("ends with expected value", () => {
        expect(workspace.generatedMakeDir).toMatch(/src\/NodeJS-Brazil\/build\/generated-make/);
    });
});
describe(".commonJsLibDir", () => {
    test("is a string", () => {
        expect(typeof workspace.commonJsLibDir).toBe("string");
    });
    test("is an absolute path", () => {
        expect((0, path_1.isAbsolute)(workspace.commonJsLibDir)).toBe(true);
    });
    test("ends with expected value", () => {
        expect(workspace.commonJsLibDir).toMatch(/src\/NodeJS-Brazil\/build\/lib\/commonjs/);
    });
});
describe(".tmpDir", () => {
    test("is a string", () => {
        expect(typeof workspace.tmpDir).toBe("string");
    });
    test("is an absolute path", () => {
        expect((0, path_1.isAbsolute)(workspace.tmpDir)).toBe(true);
    });
    test("ends with expected value", () => {
        expect(workspace.tmpDir).toMatch(/src\/NodeJS-Brazil\/build\/private\/tmp/);
    });
    test("exists as a directory", () => {
        expect((0, fs_1.statSync)(workspace.tmpDir).isDirectory()).toBe(true);
    });
});
describe(".workspaceRoot", () => {
    test("is a string", () => {
        expect(typeof workspace.workspaceRoot).toBe("string");
    });
    test("is an absolute path", () => {
        expect((0, path_1.isAbsolute)(workspace.workspaceRoot)).toBe(true);
    });
    test("exists as a directory", () => {
        expect((0, fs_1.statSync)(workspace.workspaceRoot).isDirectory()).toBe(true);
    });
});
describe(".packageName", () => {
    test("is a string", () => {
        expect(typeof workspace.packageName).toBe("string");
    });
    test("is expected value", () => {
        expect(workspace.packageName).toBe("NodeJS-Brazil");
    });
});
describe(".packageMajorVersion", () => {
    test("is a string", () => {
        expect(typeof workspace.packageMajorVersion).toBe("string");
    });
    test("is expected value", () => {
        expect(workspace.packageMajorVersion).toBe("2.0");
    });
});
describe(".packageIdentifier", () => {
    test("is a string", () => {
        expect(typeof workspace.packageIdentifier).toBe("string");
    });
    test("is expected value", () => {
        expect(workspace.packageIdentifier).toBe("NodeJS-Brazil-2.0");
    });
});
describe(".buildTools", () => {
    test("is an array", () => {
        expect(Array.isArray(workspace.buildTools)).toBeTruthy();
    });
    test("includes at least one item", () => {
        expect(workspace.buildTools.length).toBeGreaterThan(0);
    });
    test("includes only absolute paths", () => {
        workspace.buildTools.forEach((path) => expect((0, path_1.isAbsolute)(path)).toBe(true));
    });
});
