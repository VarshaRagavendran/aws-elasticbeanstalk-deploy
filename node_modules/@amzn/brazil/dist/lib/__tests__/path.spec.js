"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const child_process = __importStar(require("child_process"));
const path_1 = require("../path");
jest.mock("child_process", () => ({
    __esModule: true,
    ...jest.requireActual("child_process"),
}));
describe(".runPathRecipe()", () => {
    test("gets package name", () => expect((0, path_1.runPathRecipe)("[NodeJS]pkg.name")).resolves.toBe("NodeJS"));
    test("throws error", () => expect((0, path_1.runPathRecipe)("[NodeJS]pkg.naem")).rejects.toThrow());
    describe("memoization", () => {
        const execFileSpy = jest.spyOn(child_process, "execFile");
        const originalCwd = process.cwd();
        beforeEach(() => {
            jest.clearAllMocks();
            (0, path_1.clearCache)();
            process.env.BRAZIL_PATH_MEMOIZATION_DISABLED = undefined;
        });
        afterEach(() => {
            process.chdir(originalCwd);
        });
        test("enabled by default", async () => {
            (0, path_1.runPathRecipe)("pkg.name"); // await omitted intentionally to ensure Promise reuse
            expect(execFileSpy).toHaveBeenCalledTimes(1);
            await expect((0, path_1.runPathRecipe)("pkg.name")).resolves.toBe("NodeJS-Brazil");
            await expect((0, path_1.runPathRecipe)("pkg.name")).resolves.toBe("NodeJS-Brazil");
            expect(execFileSpy).toHaveBeenCalledTimes(1);
        });
        test.each(["true", "1"])("disabled if BRAZIL_PATH_MEMOIZATION_DISABLED = '%s'", async (memoDisabledEnvVar) => {
            process.env.BRAZIL_PATH_MEMOIZATION_DISABLED = memoDisabledEnvVar;
            await expect((0, path_1.runPathRecipe)("pkg.name")).resolves.toBe("NodeJS-Brazil");
            await expect((0, path_1.runPathRecipe)("pkg.name")).resolves.toBe("NodeJS-Brazil");
            expect(execFileSpy).toHaveBeenCalledTimes(2);
        });
        test("providing opts.memoize controls memoization", async () => {
            await expect((0, path_1.runPathRecipe)("pkg.name", { memoize: true })).resolves.toBe("NodeJS-Brazil");
            expect(execFileSpy).toHaveBeenCalledTimes(1);
            process.env.BRAZIL_PATH_MEMOIZATION_DISABLED = "true";
            await expect((0, path_1.runPathRecipe)("pkg.name", { memoize: true })).resolves.toBe("NodeJS-Brazil");
            // opts.memoize should take precedence over env variable
            expect(execFileSpy).toHaveBeenCalledTimes(1);
            await expect((0, path_1.runPathRecipe)("pkg.name", { memoize: false })).resolves.toBe("NodeJS-Brazil");
            expect(execFileSpy).toHaveBeenCalledTimes(2);
            await expect((0, path_1.runPathRecipe)("pkg.name", { memoize: false })).resolves.toBe("NodeJS-Brazil");
            expect(execFileSpy).toHaveBeenCalledTimes(3);
        });
        test("propagates sync cache", async () => {
            const execFileSyncSpy = jest.spyOn(child_process, "execFileSync");
            await (0, path_1.runPathRecipe)("pkg.name");
            expect((0, path_1.runPathRecipeSync)("pkg.name")).toBe("NodeJS-Brazil");
            expect(execFileSyncSpy).toHaveBeenCalledTimes(0);
        });
        test("scoped per working directory", async () => {
            await (0, path_1.runPathRecipe)("pkg.name");
            await (0, path_1.runPathRecipe)("pkg.name");
            expect(execFileSpy).toHaveBeenCalledTimes(1);
            // changing working directory should change the scope of memoization
            process.chdir("src");
            await (0, path_1.runPathRecipe)("pkg.name");
            await (0, path_1.runPathRecipe)("pkg.name");
            expect(execFileSpy).toHaveBeenCalledTimes(2);
        });
    });
});
describe(".runPathRecipeSync()", () => {
    test("gets package name", () => {
        expect((0, path_1.runPathRecipeSync)("[NodeJS]pkg.name")).toBe("NodeJS");
    });
    test("throws error", () => {
        expect(() => (0, path_1.runPathRecipeSync)("[NodeJS]pkg.naem")).toThrow();
    });
    describe("memoization", () => {
        const execFileSyncSpy = jest.spyOn(child_process, "execFileSync");
        const originalCwd = process.cwd();
        beforeEach(() => {
            jest.clearAllMocks();
            (0, path_1.clearCache)();
            process.env.BRAZIL_PATH_MEMOIZATION_DISABLED = undefined;
        });
        afterEach(() => {
            process.chdir(originalCwd);
        });
        test("enabled by default", () => {
            expect((0, path_1.runPathRecipeSync)("pkg.name")).toBe("NodeJS-Brazil");
            expect((0, path_1.runPathRecipeSync)("pkg.name")).toBe("NodeJS-Brazil");
            expect(execFileSyncSpy).toHaveBeenCalledTimes(1);
        });
        test.each(["true", "1"])("disabled if BRAZIL_PATH_MEMOIZATION_DISABLED = '%s'", (memoDisabledEnvVar) => {
            process.env.BRAZIL_PATH_MEMOIZATION_DISABLED = memoDisabledEnvVar;
            expect((0, path_1.runPathRecipeSync)("pkg.name")).toBe("NodeJS-Brazil");
            expect((0, path_1.runPathRecipeSync)("pkg.name")).toBe("NodeJS-Brazil");
            expect(execFileSyncSpy).toHaveBeenCalledTimes(2);
        });
        test("providing opts.memoize controls memoization", async () => {
            expect((0, path_1.runPathRecipeSync)("pkg.name", { memoize: true })).toBe("NodeJS-Brazil");
            expect(execFileSyncSpy).toHaveBeenCalledTimes(1);
            process.env.BRAZIL_PATH_MEMOIZATION_DISABLED = "true";
            expect((0, path_1.runPathRecipeSync)("pkg.name", { memoize: true })).toBe("NodeJS-Brazil");
            // opts.memoize should take precedence over env variable
            expect(execFileSyncSpy).toHaveBeenCalledTimes(1);
            expect((0, path_1.runPathRecipeSync)("pkg.name", { memoize: false })).toBe("NodeJS-Brazil");
            expect(execFileSyncSpy).toHaveBeenCalledTimes(2);
            expect((0, path_1.runPathRecipeSync)("pkg.name", { memoize: false })).toBe("NodeJS-Brazil");
            expect(execFileSyncSpy).toHaveBeenCalledTimes(3);
        });
        test("propagates async cache", async () => {
            const execFileSpy = jest.spyOn(child_process, "execFile");
            (0, path_1.runPathRecipeSync)("pkg.name");
            await expect((0, path_1.runPathRecipe)("pkg.name")).resolves.toBe("NodeJS-Brazil");
            expect(execFileSpy).toHaveBeenCalledTimes(0);
        });
        test("scoped per working directory", () => {
            (0, path_1.runPathRecipeSync)("pkg.src");
            (0, path_1.runPathRecipeSync)("pkg.src");
            expect(execFileSyncSpy).toHaveBeenCalledTimes(1);
            // changing working directory should change the scope of memoization
            process.chdir("src");
            (0, path_1.runPathRecipeSync)("pkg.src");
            (0, path_1.runPathRecipeSync)("pkg.src");
            expect(execFileSyncSpy).toHaveBeenCalledTimes(2);
        });
    });
});
