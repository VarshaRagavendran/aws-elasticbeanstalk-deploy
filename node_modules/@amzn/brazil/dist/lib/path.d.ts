export declare function runPathRecipe(recipe: string, opts?: {
    memoize?: boolean;
}): Promise<string>;
export declare function runPathRecipeSync(recipe: string, opts?: {
    memoize?: boolean;
}): string;
/**
 * Used for unit testing purposes only.
 * Do not export to customers.
 */
export declare function clearCache(): void;
